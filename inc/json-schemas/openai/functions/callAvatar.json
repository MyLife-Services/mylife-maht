{
  "name": "callAvatar",
  "description": "Call Member Avatar when request is out-of-scope for underlying intelligence, or specifically asked by member",
  "strict": true,
  "parameters": {
    "additionalProperties": false,
    "type": "object",
    "properties": {
      "botId": {
        "description": "The MyLife UUID of the external agent to call",
        "format": "uuid",
        "type": "string"
      },
      "request": {
        "description": "The original out-of-scope request from the member",
        "type": "string"
      }
    },
    "required": [
      "botId",
      "request"
    ]
  }
}
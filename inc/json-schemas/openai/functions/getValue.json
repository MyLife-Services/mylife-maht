{
    "description": "Gets the `value` summary or document by itemId",
    "name": "getValue",
    "strict": true,
    "parameters": {
        "additionalProperties": false,
        "type": "object",
        "properties": {
            "itemId": {
                "description": "Id of `value` summary to retrieve from server",
                "type": "string"
            },
            "summaryOnly": {
                "default": true,
                "description": "Whether to return only the `value` summary text or the full `item` document with summary and metadata",
                "type": "boolean"
            }
        },
        "required": [
            "itemId",
            "summaryOnly"
        ]
    }
}
{
    "annotations": {
      "title": "Obscure-Summary",
      "readOnlyHint": true,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "description": "Foundational instructions for how obscuration is interpreted by MyLife. This function obscures a summary so that no human names are present. It consumes text content and removes human name references, replacing them with the first letter of the name capitalized. At least one of 'obscuredSummary' or 'itemId' must be provided.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "forceServer": {
          "default": false,
          "description": "If true, forces the use of the server for obscuration, leave `obscuredSummary` blank.",
          "type": "boolean"
        },
        "itemId": {
          "description": "An optional GUID that references a context in which the obscuration should be interpreted. Required if 'obscuredSummary' is not provided.",
          "format": "uuid",
          "type": "string"
        },
        "obscuredSummary": {
          "description": "The cleaned and obscured text content, e.g., 'John Doe is at Google' becomes: 'J is at Google'",
          "example": "\"John Doe is at Google\" becomes: \"J is at Google\"",
          "type": "string"
        }
      },
      "required": [
        "itemId"
      ]
    },
    "name": "obscure",
    "mylife_bots": [
      "any"
    ]
  }
  